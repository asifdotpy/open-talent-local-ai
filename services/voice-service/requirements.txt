# Web Framework
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9

# Logging
loguru==0.7.2

# Production Speech Processing (ONNX Runtime - No PyTorch/CUDA!)
vosk==0.3.45              # STT - Lightweight Kaldi-based speech recognition
piper-tts==1.2.0          # TTS - Neural text-to-speech with ONNX
onnxruntime==1.17.1       # ONNX Runtime for production inference

# OpenAI TTS API
openai==1.51.2            # OpenAI API client for TTS (optional)

# Audio Processing
soundfile==0.12.1         # Audio file I/O
# Python 3.12-compatible wheels to avoid distutils build error
numpy==2.1.3              # Numerical computing (py312 wheels available)
pydub==0.25.1             # Audio manipulation
scipy==1.11.4             # Signal processing (compatible with numpy 2.1)

# Optional: Enhanced noise reduction
noisereduce==3.0.0        # Noise reduction algorithms

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0
pytest-cov==7.0.0

# Note: PyTorch and Whisper are NOT required for production!
# This service uses lightweight, CPU-only inference for deployment efficiency.

# Build tools (ensure wheels install cleanly)
setuptools>=68.0.0
wheel>=0.41.0
